[pytest]
testpaths = .
markers =
    integration: marks tests as integration tests requiring a running Flask app
    e2e: marks tests as end-to-end tests requiring a running app + Monero testnet
addopts = -v --tb=short
